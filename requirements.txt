# 故意是空的：这个项目只用 Python 标准库，一个第三方依赖都没有。
#
# 留着这个文件是因为 Railway / Render 这类 PaaS 靠它识别「这是个 Python 项目」，
# 没有它构建会失败。删掉之前先确认部署平台还能认出来。
#
# 零依赖是刻意的设计：独立服务器、云平台、本地定时任务、扣子代码节点
# 都能直接跑，不需要 pip install，也不会有依赖版本冲突。
