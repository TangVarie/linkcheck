"""小红书 / 抖音笔记评论监控，回写飞书多维表格。

模块分层，刻意让核心逻辑不依赖网络：

    links / protocol / analyze / tags / rows   纯函数，可单测，离线毫秒级跑完
    transport / feishu                          唯一碰网络的地方
    runner                                      编排：读表 → 调接口 → 合并 → 写回

这样换运行时（独立服务 / 本地定时任务）只需要换传输层，
判定口径永远只有一份，不会出现「白天点按钮和夜里批量跑结论不一致」。
"""

__version__ = "0.1.0"
